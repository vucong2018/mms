const roleReducer = (state = null, action = null) => {
    switch (action.type) {
        default:
            return state;
    }
};
const redux = {
    reducers: { role: roleReducer },
};

export default { redux };