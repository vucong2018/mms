
const userReducer = (state = null, action = null) => {
    switch (action.type) {
        default:
            return state;
    }
};

const redux = {
    reducers: { user: userReducer },
};

export default { redux };